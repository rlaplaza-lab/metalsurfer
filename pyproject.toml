[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "metalsurfer"
version = "0.4.0"
description = "Adsorption on arbitrary materials"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "metalsurfer contributors" }]
keywords = ["adsorption", "screening", "catalysis", "ase", "mlip"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Chemistry",
]
dependencies = [
    "numpy>=1.26,<2.3",
    "ase",
    "spglib",
    "pandas",
    "pyyaml>=6.0",
    "rdkit>=2024.9,<2026.3.3",
    "scipy",
    "scikit-learn",
]

[project.optional-dependencies]
mlip = [
    "torch",
    "torch-sim-atomistic>=0.5.2",
    "fairchem-core>=2.7",
    "fairchem-data-oc",
    "setuptools>=65,<82",
]
dev = [
    "pytest",
    "pytest-cov",
    "coverage",
    "ruff",
    "mypy",
    "pandas-stubs",
    "scipy-stubs",
    "types-PyYAML",
]
docs = [
    "sphinx>=7.0",
    "furo",
    "sphinx-autodoc-typehints",
    "sphinx-copybutton",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
metalsurfer = ["py.typed"]

[tool.mypy]
python_version = "3.12"
warn_redundant_casts = true
warn_unused_ignores = true
show_error_codes = true

[[tool.mypy.overrides]]
module = ["fairchem.*", "torch_sim.*", "torch.*", "sklearn.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["rdkit", "rdkit.*"]
follow_imports = "skip"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = [
    "metalsurfer.placement.site_enumeration",
    "metalsurfer.placement.site_coords",
    "metalsurfer.placement.site_voronoi",
    "metalsurfer.placement.site_classify",
]
disable_error_code = ["misc", "has-type", "union-attr", "arg-type", "attr-defined"]

[tool.ruff]
target-version = "py312"
line-length = 88
exclude = [".git", ".pytest_cache", "__pycache__", "*.egg-info", "results_*", "results", ".ruff_cache", "do_not_stage_tests"]

 [tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "F",    # pyflakes
    "I",    # isort (import sorting)
    "B",    # bugbear
    "UP",   # pyupgrade
    "SIM",  # simplify
    "ERA",  # commented-out code
    "RET",  # unnecessary return / else
    "RUF100",  # stale noqa
    "FURB105",  # print("") -> print()
    "PGH003",  # blanket type: ignore
]
ignore = ["E501"]  # line length handled by formatter

[tool.ruff.lint.per-file-ignores]
"scripts/**/*.py" = ["T201", "EXE001", "E402"]
"examples/**/*.py" = ["T201", "EXE001"]
"tests/**/*.py" = ["S101"]

[tool.ruff.format]
quote-style = "double"
